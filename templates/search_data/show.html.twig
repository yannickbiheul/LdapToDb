{% extends 'base.html.twig' %}

{% block title %}SearchData{% endblock %}

{% block body %}
    <h1>SearchData</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ search_datum.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_search_data_index') }}">back to list</a>

    <a href="{{ path('app_search_data_edit', {'id': search_datum.id}) }}">edit</a>

    {{ include('search_data/_delete_form.html.twig') }}
{% endblock %}
